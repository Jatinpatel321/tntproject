diff --git a/node_modules/react-native-gesture-handler/android/src/main/java/com/swmansion/gesturehandler/BaseReactPackage.java b/node_modules/react-native-gesture-handler/android/src/main/java/com/swmansion/gesturehandler/BaseReactPackage.java
new file mode 100644
index 0000000..84c1f11
--- /dev/null
+++ b/node_modules/react-native-gesture-handler/android/src/main/java/com/swmansion/gesturehandler/BaseReactPackage.java
@@ -0,0 +1,28 @@
+package com.swmansion.gesturehandler;
+
+import com.facebook.react.ReactPackage;
+import com.facebook.react.bridge.NativeModule;
+import com.facebook.react.bridge.ReactApplicationContext;
+import com.facebook.react.uimanager.ViewManager;
+import java.util.List;
+
+/**
+ * Compatibility shim so React Native 0.73 autolinking can instantiate
+ * the gesture handler package even though upstream renamed the
+ * exported class.
+ */
+public class BaseReactPackage implements ReactPackage {
+  private final RNGestureHandlerPackage delegate = new RNGestureHandlerPackage();
+
+  @Override
+  public List<NativeModule> createNativeModules(ReactApplicationContext reactContext) {
+    return delegate.createNativeModules(reactContext);
+  }
+
+  @SuppressWarnings({"rawtypes", "unchecked"})
+  @Override
+  public List<ViewManager> createViewManagers(ReactApplicationContext reactContext) {
+    return (List) delegate.createViewManagers(reactContext);
+  }
+
+}
